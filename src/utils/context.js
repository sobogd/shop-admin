import React from "react";

/**
 * Контекст для показа спиннера загрузки.
 */
export const Loading = React.createContext(null);

/**
 * Контекст для показа ошибок.
 */
export const Error = React.createContext(null);
